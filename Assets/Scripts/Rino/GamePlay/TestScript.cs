using System;
using Newtonsoft.Json;
using Nino.Shared;
using Rino.Shared.Data;
using UnityEngine;

namespace Rino.GamePlay
{
    public class TestScript : MonoBehaviour
    {
        public RinoChart testChart;
        /*private void Awake()
        {
            var list = new UncheckedList<int>();
            print(JsonConvert.SerializeObject(list));
        }*/
    }
}
